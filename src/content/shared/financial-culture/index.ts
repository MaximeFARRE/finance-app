import type { Card } from "@/lib/types";

// Cross-track cards (financial culture, major crises, etc.)
// These cards are not bound to a specific track (trackId: undefined).
// Add cards here that are relevant across multiple tracks.
export const financialCultureCards: Card[] = [];
